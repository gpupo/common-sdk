sudo: false
language: php
os:
  - linux
php:
  - 7.2
  - nightly
matrix:
  allow_failures:
    - php: nightly
branches:
  except:
    - /^psr.*$/
    - /^dev.*$/
    - /^topic.*$/
script: ant travis
notifications:
  email: false
