<?php

/*
 * This file is autogenerated at {{ "now"|date("m/d/Y g:ia") }} by gpupo/common-sdk
 * @see <http://www.g1mr.com/common-sdk/>
 */

namespace {{testNamespace}};

use Gpupo\CommonSdk\Entity\EntityInterface;
use Gpupo\Tests\CommonSdk\Traits\EntityTrait;
use {{testAbstract}};
use {{class}};

{{ block|trim }}
class {{ classShortName }}Test extends TestCaseAbstract
{
{% if entity %}
    use EntityTrait;

    const QUALIFIED = {{ classShortName }}::class;

    public static function setUpBeforeClass()
    {
        static::setFullyQualifiedObject(self::QUALIFIED);
        parent::setUpBeforeClass();
    }

{% endif %}

    /**
     * @return \{{class}}
     */
    public function dataProvider{{ classShortName }}()
    {
{% if entity %}
    {{ expected|raw }}
        return $this->dataProviderEntitySchema(self::QUALIFIED, $expected);
{% else %}
        return [[new {{ classShortName }}()]];
{% endif %}
    }
    {{ asserts|raw }}

}
